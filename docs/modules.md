# dx modules

List all discovered modules.

## Usage

```bash
dx modules                  # List modules with brief description
dx modules -v               # Verbose: include path and compose file
```

Modules are discovered from the `modules/` directory (or `DX_MODULES_DIR` if set).

## Examples

```bash
dx modules                  # All modules
dx modules -v               # With path and compose file
```
